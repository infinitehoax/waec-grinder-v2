// ============================================
// WAEC GRINDER — Frontend Config
// ============================================

const APP_CONFIG = {
  BATCH_SIZE: 5,          // Questions per batch
  TIMED_BATCH_SIZE: 50,   // Questions for a timed session
  PASS_THRESHOLD: 0.5,    // 50% to pass a theory question
  API_BASE: '/api',
};

export default APP_CONFIG;
